{
  "name": "superpowers-optimized",
  "version": "6.7.0",
  "private": true,
  "description": "Build tooling for the Pi (pi.dev) extension. Skills/hooks for Claude/Codex/OpenCode require no Node tooling.",
  "scripts": {
    "build": "tsc -p .",
    "build:watch": "tsc -p . --watch",
    "test:pi": "bash tests/pi/run-tests.sh",
    "test:smart-compress": "bash tests/smart-compress/run-tests.sh",
    "test:subagent-mode": "bash tests/subagent-mode/run-tests.sh"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^26.0.0",
    "js-yaml": "^5.0.0",
    "typescript": "^5.5.0"
  }
}
